此为程序主文件夹

其中

  make.cmd 主脚本
  rule-list.ini 被处理的上游规则链接列表（自行修改后可以添加或删除规则）
  
  purge.cmd 用于刷新 jsd 缓存（并没有什么用）（ fork 后根据需要修改仓库名）
  remove.cmd 运行后删除残留文件（仅为 Actions ，在本地电脑运行时不要使用这个脚本，后果自负）
  
  aa 运行需要的文件夹 详见内部说明
  
